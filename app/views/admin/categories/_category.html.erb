<tr id="category-<%= category.id %>">
  <td><%= category.name %></td>
  <td><%= category.description %></td>
  <td><%= link_to t("delete"), admin_category_path(category), remote: true, method: :delete,
    data: {confirm: t("confirm_action", action: t("delete"))} %>
  </td>
</tr>
